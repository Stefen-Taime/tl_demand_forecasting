apiVersion: 1

datasources:
  - name: PostgreSQL-Predictions
    uid: PREDICTIONS_DS
    type: postgres
    access: proxy
    url: localhost:5432
    user: mlflow
    jsonData:
      database: predictions
      sslmode: disable
      postgresVersion: 1400
      timescaledb: false
    secureJsonData:
      password: "{{ mlflow_db_password }}"
